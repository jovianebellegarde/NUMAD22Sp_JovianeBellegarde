<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="edu.neu.numad22sp_jovianebellegarde">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher_jb"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_jb_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.NUMAD22Sp_JovianeBellegarde">
        <activity
                android:name=".LocationActivity"
                android:exported="false" />
        <activity
                android:name=".LinkListActivity"
                android:exported="false" />
        <activity
                android:name=".AboutMeActivity"
                android:exported="false" />
        <activity android:name=".ButtonActivity" />
        <activity
                android:name=".MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>